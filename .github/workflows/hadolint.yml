name: Hadolint

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  hadolint:
    name: Lint Containerfiles
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run Hadolint
        id: hadolint
        uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: Containerfile

      - name: Write lint report
        if: always()
        run: |
          {
            echo "Hadolint checked: Containerfile"
            echo "Result: ${{ steps.hadolint.outcome }}"
          } >> "$GITHUB_STEP_SUMMARY"

  notify:
    name: Notify Discord
    needs: hadolint
    if: ${{ always() }}
    uses: ./.github/workflows/notify-discord.yml
    with:
      workflow_name: ${{ github.workflow }}
      status: ${{ needs.hadolint.result }}
      run_url: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
      ref_name: ${{ github.ref_name }}
      sha: ${{ github.sha }}
      event_name: ${{ github.event_name }}
      report: "Hadolint checked Containerfile — result: ${{ needs.hadolint.result }}"
      report_url: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
    secrets: inherit
